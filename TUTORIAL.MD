1. py -m venv venv (creates the env folder in the terminal)
2. venv\Scripts\Activate.ps1 (activates the env in powershell)
3. cd C:\Users\fabia\Desktop\Personal Projects\jomble-app (navigates to the project folder)
4. Run any command starting with py